<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Invoice - CoffeeStream</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 20px auto;
      background-color: #ffffff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .header {
      text-align: center;
      padding: 10px 0;
      border-bottom: 1px solid #dddddd;
    }
    .header h1 {
      margin: 0;
      color: #333333;
    }
    .content {
      padding: 20px 0;
    }
    .content p {
      color: #555555;
      line-height: 1.6;
    }
    .invoice-details {
      margin: 20px 0;
    }
    .invoice-details table {
      width: 100%;
      border-collapse: collapse;
    }
    .invoice-details table th,
    .invoice-details table td {
      padding: 10px;
      border: 1px solid #dddddd;
    }
    .invoice-details th {
      background-color: #f8f8f8;
    }
    .footer {
      text-align: center;
      padding: 10px 0;
      border-top: 1px solid #dddddd;
      color: #777777;
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Invoice #{{ invoiceNumber }}</h1>
    </div>
    <div class="content">
      <p>Hello {{ username }},</p>
      <p>Thank you for your purchase! Below are the details of your invoice:</p>

      <div class="invoice-details">
        <table>
          <thead>
            <tr>
              <th>Product</th>
              <th>Quantity</th>
              <th>Price</th>
              <th>Total</th>
            </tr>
          </thead>
          <tbody>
            @each(product in products)
            <tr>
              <td>{{ product.title }}</td>
              <td>{{ product.quantity }}</td>
              <td>${{ product.unit_price }}</td>
              <td>${{ product.totalPrice }}</td>
            </tr>
            @endeach
          </tbody>
        </table>
      </div>

      @if(discounts && discounts.length > 0)
      <div class="discounts">
        <h4>Discounts Applied:</h4>
        <ul>
          @each(discount in discounts)
          <li>
            <strong>{{ discount.name }}:</strong> -${{ discount.applied_amount }}
          </li>
          @endeach
        </ul>
      </div>
      @endif

      <p>Total Amount: <strong>${{ total }}</strong></p>
      <p>Date of Purchase: {{ purchaseDate }}</p>
    </div>
    <div class="footer">
      <p>&copy; 2024 CoffeeStream. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
